# Fresh IEEE conference draft

`main.tex` is the current manuscript written from code, configuration, audited experiment artifacts, and the four approved figures.

Compile the complete paper from the repository root:

```powershell
.\compile_paper.ps1
```

The named output is written to `output/pdf/REIM_main_four_seed.pdf`. Before
submission, select the exact IEEE conference template and have the human authors
verify the manuscript, experimental claims, and references.
